# My React App

## 📝 Опис проекту

Це базовий React проект з TypeScript, створений за допомогою Vite, який демонструє роботу з функціональними компонентами, props та обробниками подій. Проект включає два переосяжні компоненти: **Button** (кнопка) та **Input** (текстове поле), які можна використовувати в різних частинах додатку.

## ✨ Особливості

- ⚛️ React з TypeScript
- ⚡ Vite для швидкої розробки
- 🎨 Власні компоненти Button та Input з типізацією
- 🔄 Обробка подій (onClick, onChange)
- 📦 Модульна структура коду
- 🚀 Готовий до деплою

## 🛠️ Технології

- **React** - бібліотека для створення користувацьких інтерфейсів
- **TypeScript** - типізована надбудова над JavaScript
- **Vite** - інструмент для швидкої розробки
- **ESLint** - лінтер для перевірки якості коду

## 📁 Структура проекту

```
my-react-app/
├── src/
│   ├── components/
│   │   ├── Button.tsx      # Компонент кнопки
│   │   └── Input.tsx       # Компонент поля введення
│   ├── App.tsx             # Головний компонент додатку
│   ├── main.tsx            # Точка входу
│   ├── App.css             # Стилі App компонента
│   ├── index.css           # Глобальні стилі
│   └── assets/             # Статичні ресурси
├── public/                 # Публічні файли
│   └── vite.svg            # Vite логотип
├── index.html              # HTML шаблон
├── package.json            # Залежності проекту
├── tsconfig.json           # Конфігурація TypeScript
├── vite.config.ts          # Конфігурація Vite
└── README.md               # Документація проекту
```

## 🚀 Встановлення та запуск

### Передумови

Переконайтеся, що у вас встановлено:

- Node.js (версія 14.0 або вище)
- npm або yarn

### Інструкція з встановлення

1. **Клонуйте репозиторій (або використовуйте існуючий проект):**

   ```bash
   git clone <посилання-на-ваш-репозиторій>
   cd my-react-app
   ```

2. **Встановіть залежності:**

   ```bash
   npm install
   ```

3. **Запустіть проект в режимі розробки:**

   ```bash
   npm run dev
   ```

4. **Відкрийте браузер:**

   Перейдіть за адресою [http://localhost:5173](http://localhost:5173)

## 📦 Скрипти

- `npm run dev` - запуск проекту в режимі розробки
- `npm run build` - збірка проекту для продакшену
- `npm run preview` - перегляд production збірки локально

## 🎯 Опис компонентів

### Button Component

Переосяжний компонент кнопки з підтримкою різних типів та обробників подій.

**Props:**

- `text` (string) - текст, який відображається на кнопці
- `type` (string, optional) - тип кнопки (`button`, `submit`, `reset`), за замовчуванням `button`
- `onClick` (function) - функція-обробник кліку

**Приклад використання:**

```tsx
<Button
  text="Натисни мене"
  type="button"
  onClick={() => console.log("Кнопка натиснута!")}
/>
```

### Input Component

Переосяжний компонент поля введення з підтримкою різних типів та обробників змін.

**Props:**

- `placeholder` (string, optional) - підказка для користувача
- `type` (string, optional) - тип поля (`text`, `password`, `email`, `number`), за замовчуванням `text`
- `onChange` (function) - функція-обробник зміни значення
- `value` (string, optional) - значення поля (для контрольованих компонентів)

**Приклад використання:**

```tsx
<Input
  placeholder="Введіть ваше ім'я"
  type="text"
  value={inputValue}
  onChange={(e) => setInputValue(e.target.value)}
/>
```

## 🌐 Демо

🔗 **Live Demo:** [Посилання на демо-версію проекту](https://your-demo-link.vercel.app)

_(Оновіть це посилання після деплойменту)_

## 📤 Деплоймент

Проект готовий до розгортання на платформі **Vercel** або **Netlify**.

### Деплой на Vercel:

1. Встановіть Vercel CLI:

   ```bash
   npm install -g vercel
   ```

2. Запустіть деплой:
   ```bash
   vercel
   ```

### Деплой на Netlify:

1. Встановіть Netlify CLI:

   ```bash
   npm install -g netlify-cli
   ```

2. Виконайте збірку:

   ```bash
   npm run build
   ```

3. Запустіть деплой:
   ```bash
   netlify deploy --prod
   ```

## 📝 Що було реалізовано

- ✅ Ініціалізація React проекту з TypeScript через Vite
- ✅ Створення компонента Button з props (text, type, onClick)
- ✅ Створення компонента Input з props (placeholder, type, onChange, value)
- ✅ Типізація всіх компонентів за допомогою TypeScript
- ✅ Використання компонентів в App.tsx з прикладами
- ✅ Обробка подій користувача (onClick, onChange)
- ✅ Управління станом з useState
- ✅ Тестування в браузері
- ✅ Готовність до деплойменту
- ✅ Документація проекту

## 🎓 Навчальні цілі

Цей проект допоміг освоїти:

- Створення React проекту з Vite та TypeScript
- Роботу з функціональними компонентами
- Передачу та використання props з типізацією
- Обробку подій в React
- Управління станом за допомогою хуків (useState)
- Структурування коду та розділення компонентів
- Створення переосяжних (reusable) компонентів
- Деплоймент React додатків

## 💡 Приклади використання

### Форма з валідацією

```tsx
const [name, setName] = useState("");
const [password, setPassword] = useState("");

const handleSubmit = () => {
  if (name && password) {
    alert(`Привіт, ${name}!`);
  }
};

return (
  <div>
    <Input
      placeholder="Ім'я користувача"
      type="text"
      value={name}
      onChange={(e) => setName(e.target.value)}
    />
    <Input
      placeholder="Пароль"
      type="password"
      value={password}
      onChange={(e) => setPassword(e.target.value)}
    />
    <Button text="Увійти" type="submit" onClick={handleSubmit} />
  </div>
);
```

## 📚 Корисні ресурси

- [React Documentation](https://react.dev/)
- [Vite Documentation](https://vitejs.dev/)
- [TypeScript Documentation](https://www.typescriptlang.org/)
- [React TypeScript Cheatsheet](https://react-typescript-cheatsheet.netlify.app/)

## 👨‍💻 Автор

Ваше ім'я

## 📄 Ліцензія

MIT License - використовуйте проект як завгодно!

---

⭐ Якщо проект був корисним, поставте зірочку на GitHub!
